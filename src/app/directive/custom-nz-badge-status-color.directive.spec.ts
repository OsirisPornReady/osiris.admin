import { CustomNzBadgeStatusColorDirective } from './custom-nz-badge-status-color.directive';

describe('CustomNzBadgeStatusColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomNzBadgeStatusColorDirective();
    expect(directive).toBeTruthy();
  });
});
